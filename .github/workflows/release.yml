name: Build & Release

# Builda gli eseguibili a ogni tag "vX.Y.Z" e pubblica la release con gli .exe.
# Puoi anche lanciarlo a mano (workflow_dispatch): in quel caso builda soltanto
# e allega gli artefatti al run, senza creare release.
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-release:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      # Il tag è l'unica fonte di verità per la versione: allinea package.json.
      - name: Set version from tag
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        run: npm version "${GITHUB_REF_NAME#v}" --no-git-tag-version --allow-same-version

      - name: Generate icons
        run: node scripts/make-icon.js

      - name: Build installer + portable
        run: npx electron-builder --publish never

      # latest.yml e i .blockmap servono all'auto-update (electron-updater):
      # senza di essi l'app installata non si accorge della nuova versione.
      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          shopt -s nullglob
          files=(dist/*.exe dist/latest.yml dist/*.blockmap)
          echo "Asset da pubblicare:"; printf ' - %s\n' "${files[@]}"
          gh release create "$GITHUB_REF_NAME" "${files[@]}" \
            --title "Widget Spotify $GITHUB_REF_NAME" \
            --generate-notes \
          || gh release upload "$GITHUB_REF_NAME" "${files[@]}" --clobber

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: widget-spotify-exe
          path: |
            dist/*.exe
            dist/latest.yml
          if-no-files-found: error
